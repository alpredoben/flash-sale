<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <title>{{appName}}</title>
  <style type="text/css">
    /* ===== RESET & BASE ===== */
    * { margin: 0; padding: 0; box-sizing: border-box; }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      background-color: #f0f4f8;
      color: #334155;
      line-height: 1.6;
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
    }

    a { color: #6366f1; text-decoration: none; }
    a:hover { color: #4f46e5; text-decoration: underline; }

    img { max-width: 100%; height: auto; }

    /* ===== OUTER WRAPPER ===== */
    .email-wrapper {
      max-width: 600px;
      margin: 32px auto;
      background: #ffffff;
      border-radius: 16px;
      overflow: hidden;
      box-shadow: 0 4px 24px rgba(0, 0, 0, 0.07);
      border: 1px solid #e2e8f0;
    }

    /* ===== HEADER ===== */
    .email-header {
      background: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
      padding: 36px 32px;
      text-align: center;
    }
    .email-header .logo-text {
      font-size: 26px;
      font-weight: 700;
      color: #ffffff;
      letter-spacing: -0.5px;
    }
    .email-header .logo-sub {
      font-size: 13px;
      color: rgba(255,255,255,0.75);
      margin-top: 4px;
      letter-spacing: 1.5px;
      text-transform: uppercase;
    }

    /* ===== ICON BADGE (centered, above body title) ===== */
    .icon-badge-wrap {
      text-align: center;
      margin-top: -36px;
      position: relative;
      z-index: 1;
    }
    .icon-badge {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: 72px;
      height: 72px;
      border-radius: 50%;
      background: #ffffff;
      box-shadow: 0 4px 16px rgba(99, 102, 241, 0.25);
      border: 4px solid #eef2ff;
    }
    .icon-badge svg { width: 32px; height: 32px; }

    /* ===== BODY ===== */
    .email-body {
      padding: 32px 36px 40px;
      text-align: center;
    }
    .email-body h1 {
      font-size: 22px;
      font-weight: 700;
      color: #1e293b;
      margin-bottom: 10px;
      letter-spacing: -0.3px;
    }
    .email-body .subtitle {
      font-size: 14px;
      color: #64748b;
      max-width: 440px;
      margin: 0 auto 28px;
      line-height: 1.7;
    }

    /* ===== CTA BUTTON ===== */
    .btn-cta {
      display: inline-block;
      background: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
      color: #ffffff !important;
      font-size: 15px;
      font-weight: 600;
      padding: 14px 36px;
      border-radius: 10px;
      text-decoration: none !important;
      letter-spacing: 0.2px;
      box-shadow: 0 4px 14px rgba(99, 102, 241, 0.4);
      transition: box-shadow 0.2s ease, transform 0.15s ease;
    }
    .btn-cta:hover {
      box-shadow: 0 6px 20px rgba(99, 102, 241, 0.5);
      transform: translateY(-1px);
      text-decoration: none !important;
    }

    /* ===== DIVIDER ===== */
    .divider {
      width: 48px;
      height: 3px;
      background: linear-gradient(90deg, #6366f1, #8b5cf6);
      border-radius: 2px;
      margin: 28px auto;
    }

    /* ===== INFO CARD (for token / expiry details) ===== */
    .info-card {
      background: #f8fafc;
      border: 1px solid #e2e8f0;
      border-radius: 12px;
      padding: 20px 24px;
      margin: 0 auto 24px;
      max-width: 420px;
      text-align: left;
    }
    .info-card .info-row {
      display: flex;
      align-items: flex-start;
      gap: 12px;
      padding: 10px 0;
    }
    .info-card .info-row + .info-row {
      border-top: 1px solid #e2e8f0;
    }
    .info-card .info-icon {
      flex-shrink: 0;
      width: 36px;
      height: 36px;
      border-radius: 8px;
      background: #eef2ff;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .info-card .info-icon svg { width: 18px; height: 18px; color: #6366f1; }
    .info-card .info-label {
      font-size: 11px;
      font-weight: 600;
      color: #94a3b8;
      text-transform: uppercase;
      letter-spacing: 0.8px;
      margin-bottom: 2px;
    }
    .info-card .info-value {
      font-size: 13px;
      color: #334155;
      font-weight: 500;
      word-break: break-all;
    }

    /* ===== LINK FALLBACK (for copy-paste URL) ===== */
    .link-fallback {
      margin: 24px auto 0;
      max-width: 440px;
      text-align: center;
    }
    .link-fallback p {
      font-size: 12px;
      color: #94a3b8;
      margin-bottom: 8px;
    }
    .link-fallback .link-box {
      background: #f1f5f9;
      border: 1px solid #e2e8f0;
      border-radius: 8px;
      padding: 10px 14px;
      font-size: 11.5px;
      color: #6366f1;
      word-break: break-all;
      text-align: left;
      font-family: 'SF Mono', 'Fira Code', 'Consolas', monospace;
    }

    /* ===== SECURITY NOTE ===== */
    .security-note {
      background: #fefce8;
      border: 1px solid #fde047;
      border-radius: 10px;
      padding: 14px 18px;
      margin: 24px auto 0;
      max-width: 440px;
      display: flex;
      align-items: flex-start;
      gap: 10px;
      text-align: left;
    }
    .security-note .sec-icon { flex-shrink: 0; }
    .security-note .sec-icon svg { width: 20px; height: 20px; color: #ca8a04; }
    .security-note p {
      font-size: 12px;
      color: #713f12;
      line-height: 1.6;
    }

    /* ===== SUCCESS CHECKMARK SECTION ===== */
    .success-section {
      text-align: center;
      padding: 8px 0 4px;
    }
    .success-circle {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: 80px;
      height: 80px;
      border-radius: 50%;
      background: linear-gradient(135deg, #10b981, #34d399);
      box-shadow: 0 4px 16px rgba(16, 185, 129, 0.35);
      margin-bottom: 20px;
    }
    .success-circle svg { width: 40px; height: 40px; color: #fff; }

    /* ===== TIPS LIST ===== */
    .tips-list {
      list-style: none;
      padding: 0;
      margin: 0 auto;
      max-width: 420px;
      text-align: left;
    }
    .tips-list li {
      display: flex;
      align-items: flex-start;
      gap: 10px;
      padding: 10px 0;
      font-size: 13px;
      color: #475569;
    }
    .tips-list li + li { border-top: 1px solid #f1f5f9; }
    .tips-list .tip-dot {
      flex-shrink: 0;
      width: 22px;
      height: 22px;
      border-radius: 6px;
      background: #eef2ff;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .tips-list .tip-dot svg { width: 13px; height: 13px; color: #6366f1; }

    /* ===== FOOTER ===== */
    .email-footer {
      background: #f8fafc;
      border-top: 1px solid #e2e8f0;
      padding: 28px 32px;
      text-align: center;
    }
    .email-footer p {
      font-size: 12px;
      color: #94a3b8;
      line-height: 1.8;
    }
    .email-footer a { color: #6366f1; font-size: 12px; }
    .email-footer .footer-links { margin-bottom: 12px; }
    .email-footer .footer-links a + a { margin-left: 16px; }

    /* ===== RESPONSIVE ===== */
    @media screen and (max-width: 600px) {
      .email-wrapper { margin: 12px 8px; border-radius: 12px; }
      .email-header { padding: 28px 20px; }
      .email-body { padding: 24px 20px 32px; }
      .email-body h1 { font-size: 20px; }
      .email-footer { padding: 22px 20px; }
      .info-card { margin-left: 0; margin-right: 0; }
      .link-fallback { margin-left: 0; margin-right: 0; }
      .security-note { margin-left: 0; margin-right: 0; }
    }
  </style>
</head>
<body>

  <table width="100%" cellpadding="0" cellspacing="0" style="background:#f0f4f8; padding: 20px 0;">
    <tr>
      <td align="center">

        <!-- OUTER SHELL (table for Outlook compat) -->
        <table width="600" max-width="600" cellpadding="0" cellspacing="0" class="email-wrapper" style="max-width:600px; width:100%; background:#fff; border-radius:16px; overflow:hidden; box-shadow:0 4px 24px rgba(0,0,0,0.07); border:1px solid #e2e8f0;">

          <!-- HEADER -->
          <tr>
            <td class="email-header" style="background:linear-gradient(135deg,#6366f1 0%,#8b5cf6 100%); padding:36px 32px; text-align:center;">
              <div class="logo-text" style="font-size:26px; font-weight:700; color:#fff; letter-spacing:-0.5px;">{{appName}}</div>
              <div class="logo-sub" style="font-size:13px; color:rgba(255,255,255,0.75); margin-top:4px; letter-spacing:1.5px; text-transform:uppercase;">Secure Notifications</div>
            </td>
          </tr>

          <!-- DYNAMIC BODY -->
          <tr>
            <td class="email-body" style="padding:32px 36px 40px; text-align:center;">
              {{{body}}}
            </td>
          </tr>

          <!-- FOOTER -->
          <tr>
            <td class="email-footer" style="background:#f8fafc; border-top:1px solid #e2e8f0; padding:28px 32px; text-align:center;">
              <div class="footer-links" style="margin-bottom:12px;">
                <a href="{{appUrl}}/privacy" style="color:#6366f1; font-size:12px;">Privacy Policy</a>
                <a href="{{appUrl}}/terms" style="color:#6366f1; font-size:12px; margin-left:16px;">Terms of Service</a>
                <a href="{{appUrl}}/support" style="color:#6366f1; font-size:12px; margin-left:16px;">Support</a>
              </div>
              <p style="font-size:12px; color:#94a3b8; line-height:1.8;">
                This email was sent to <strong>{{recipientEmail}}</strong> because you have an account with {{appName}}.<br/>
                &copy; {{currentYear}} {{appName}}. All rights reserved.
              </p>
            </td>
          </tr>

        </table>

      </td>
    </tr>
  </table>

</body>
</html>
